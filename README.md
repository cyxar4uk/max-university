# Цифровой университет MAX

Платформа-конструктор для создания цифровых пространств вузов в мессенджере MAX. Решение позволяет администраторам вузов настраивать блочную структуру приложения для разных ролей пользователей (студенты, абитуриенты, сотрудники, администраторы).

## 🎯 Концепция проекта

Платформа предоставляет **универсальный конструктор** для вузов:
- Администраторы вузов настраивают блочную структуру приложения
- Каждая роль (студент, абитуриент, сотрудник, администратор) видит только доступные ей блоки
- Блоки включают: профиль, расписание, учебные материалы, услуги, новости, аналитику и др.
- Решение масштабируемо и универсально для любых вузов

## 🏗️ Архитектура

- **Frontend**: React + Vite + VKUI (компоненты MAX)
- **Backend**: FastAPI (Python)
- **State Management**: Redux Toolkit
- **Bridge**: MAX Bridge API для интеграции с мессенджером MAX
- **Bot**: MAX Bot API для чат-бота с inline кнопками

## 📋 Требования

- **Node.js** версии 18 или выше
- **Python** версии 3.9 или выше
- **npm** или **yarn**
- **Docker** и **Docker Compose** (для запуска через контейнеры)

## 🚀 Быстрый старт

### Вариант 1: Локальный запуск

#### 1. Установка зависимостей фронтенда

```bash
npm install
```

#### 2. Установка зависимостей бэкенда

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

#### 3. Запуск бэкенда

В одном терминале:

```bash
# Активация виртуального окружения (если не активировано)
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

# Запуск сервера
python main.py
```

Бэкенд будет доступен по адресу: **http://localhost:8000**

API документация: **http://localhost:8000/docs**

#### 4. Запуск фронтенда

В другом терминале:

```bash
npm run dev
```

Фронтенд будет доступен по адресу: **http://localhost:3000**

### Вариант 2: Запуск через Docker

#### Сборка и запуск бэкенда

```bash
# Сборка образа
docker build -t max-university-backend -f Dockerfile .

# Запуск контейнера
docker run -p 8000:8000 max-university-backend
```

#### Сборка и запуск фронтенда

```bash
# Сборка образа
docker build -t max-university-frontend -f Dockerfile.frontend .

# Запуск контейнера
docker run -p 80:80 max-university-frontend
```

### Вариант 3: Запуск через Docker Compose

```bash
# Запуск всех сервисов
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

Сервисы будут доступны:
- Frontend: **http://localhost:3000**
- Backend: **http://localhost:8000**

## 📦 Структура проекта

```
max-university/
├── main.py                 # FastAPI бэкенд сервер
├── App.jsx                 # Главный компонент React
├── WelcomePage.jsx         # Страница приветствия и авторизации
├── HomePage.jsx            # Главная страница с блочной структурой
├── ProfilePage.jsx         # Страница профиля
├── SchedulePage.jsx        # Страница расписания
├── CoursePage.jsx          # Страница курса
├── AdminPage.jsx           # Панель администратора
├── userSlice.js            # Redux slice для пользователя
├── store.js                # Redux store
├── useMAXBridge.js         # Хук для работы с MAX Bridge
├── api-service.js          # Сервис для работы с API
├── Dockerfile              # Dockerfile для бэкенда
├── Dockerfile.frontend     # Dockerfile для фронтенда
├── docker-compose.yml      # Docker Compose конфигурация
├── nginx.conf              # Конфигурация Nginx для фронтенда
├── package.json            # Зависимости Node.js
├── requirements.txt        # Зависимости Python
├── vite.config.js          # Конфигурация Vite
└── README.md               # Этот файл
```

## 🔧 Переменные окружения

### Бэкенд

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=your-secret-key-change-in-production
MAX_BOT_TOKEN=your-max-bot-token-here
DATABASE_URL=postgresql://user:password@localhost/dbname
```

В `main.py` можно изменить:
- `SECRET_KEY` - ключ для валидации данных от MAX Bridge
- `MAX_BOT_TOKEN` - токен бота MAX
- `MAX_API_BASE` - базовый URL API MAX (по умолчанию: `https://api.max.ru/bot`)

### Фронтенд

Создайте файл `.env`:

```env
REACT_APP_API_URL=http://localhost:8000/api
```

## 📡 API Endpoints

### Основные

- `GET /` - Информация о API
- `GET /api/health` - Проверка здоровья сервера

### Аутентификация

- `POST /api/users/auth` - Аутентификация пользователя через MAX Bridge
- `PUT /api/users/role` - Обновление роли пользователя

### Университеты

- `GET /api/universities/{id}` - Информация об университете
- `GET /api/universities/{id}/blocks?role={role}` - Конфигурация блоков для роли

### Контент

- `GET /api/schedule?date={date}` - Расписание пользователя
- `GET /api/courses` - Список курсов
- `GET /api/events` - Список событий
- `POST /api/events/{id}/register` - Регистрация на событие
- `GET /api/news` - Новости университета

### Администрирование

- `GET /api/statistics` - Статистика университета (только для админов)

### Bot Webhook

- `POST /api/bot/webhook` - Вебхук для получения обновлений от MAX Bot

Полная документация API доступна по адресу: **http://localhost:8000/docs**

## 🎨 Функционал

### Чат-бот MAX

- Выбор роли через inline кнопки
- Главное меню с быстрыми действиями
- Уведомления и быстрые команды
- Открытие мини-приложения с передачей роли

### Мини-приложение

#### Для всех ролей:
- **Профиль** - информация о пользователе
- **Новости** - актуальные новости университета

#### Для студентов:
- **Расписание** - расписание занятий
- **Учебные материалы (LMS)** - курсы, задания, библиотека
- **Услуги** - заказ справок, документов
- **Внеучебная жизнь** - события, клубы, вакансии
- **Оплата** - оплата обучения

#### Для абитуриентов:
- **Поступление** - информация о поступлении
- **Оплата** - оплата вступительных взносов

#### Для сотрудников:
- **График** - рабочий график
- **Услуги** - заявки, документы

#### Для администраторов:
- **Аналитика** - статистика университета
- **Настройки** - конфигурация блоков
- **Пользователи** - управление пользователями

## 🐳 Docker команды

### Сборка образов

```bash
# Бэкенд
docker build -t max-university-backend -f Dockerfile .

# Фронтенд
docker build -t max-university-frontend -f Dockerfile.frontend .
```

### Запуск контейнеров

```bash
# Бэкенд
docker run -d -p 8000:8000 --name backend max-university-backend

# Фронтенд
docker run -d -p 80:80 --name frontend max-university-frontend
```

### Остановка и удаление

```bash
# Остановка
docker stop backend frontend

# Удаление
docker rm backend frontend
```

### Просмотр логов

```bash
docker logs backend
docker logs frontend
```

## 🌐 Деплой на GitHub Pages

Подробная инструкция по деплою находится в файле [DEPLOY.md](./DEPLOY.md)

Краткая версия:

1. Создайте репозиторий на GitHub
2. Загрузите код в репозиторий
3. В настройках репозитория включите GitHub Pages (Settings → Pages → Source: GitHub Actions)
4. После пуша в ветку `main` или `master` автоматически запустится деплой
5. Обновите URL в `main.py`, заменив `YOUR_USERNAME` на ваш GitHub username

URL приложения будет: `https://{username}.github.io/{repository-name}/`

## 🔐 Безопасность

⚠️ **Важно для продакшена:**

1. Измените `SECRET_KEY` в `main.py` на уникальный ключ
2. Укажите конкретные домены в CORS настройках
3. Используйте переменные окружения для секретных данных
4. Настройте HTTPS для работы с MAX Bridge
5. Замените временное хранилище на реальную БД

## 📝 Разработка

### Запуск в режиме разработки

1. **Бэкенд**: `python main.py` (автоматическая перезагрузка включена)
2. **Фронтенд**: `npm run dev` (hot reload включен)

### Сборка для продакшена

```bash
# Фронтенд
npm run build

# Результат будет в папке dist/
```

## 🧪 Тестирование

### Проверка бэкенда

```bash
# Health check
curl http://localhost:8000/api/health

# API документация
# Откройте в браузере: http://localhost:8000/docs
```

### Проверка фронтенда

Откройте в браузере: http://localhost:3000

## 📚 Дополнительная информация

- **MAX Bridge API**: Используется для интеграции с мессенджером MAX
- **VKUI**: Компоненты интерфейса от VKontakte, совместимые с MAX
- **FastAPI**: Современный веб-фреймворк для Python
- **React Router**: Маршрутизация в React приложении
- **Redux Toolkit**: Управление состоянием приложения

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в рамках хакатона.

## 👥 Команда

Проект разработан командой для трека "Цифровизация: создание цифрового вуза" в рамках хакатона MAX.

---

**Примечание**: Для работы с MAX Bridge необходимо развернуть приложение на сервере с HTTPS. В режиме разработки MAX Bridge может быть недоступен, приложение будет работать в обычном режиме.
