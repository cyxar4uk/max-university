# Инструкция по настройке проекта для хакатона

## ✅ Что уже готово

1. ✅ Docker-образы для бэкенда и фронтенда
2. ✅ Requirements.txt с описанием всех библиотек и версий
3. ✅ README с подробной инструкцией по запуску
4. ✅ GitHub Actions для автоматического деплоя на GitHub Pages
5. ✅ Полный функционал MVP мини-приложения
6. ✅ Интеграция с MAX Bridge API
7. ✅ Чат-бот с inline кнопками
8. ✅ Блочная структура для разных ролей

## 🚀 Быстрые шаги для получения HTTPS ссылки

### 1. Создайте репозиторий на GitHub

```bash
# На GitHub создайте новый репозиторий (например, max-university)
```

### 2. Загрузите код

```bash
git init
git add .
git commit -m "Initial commit: Digital University MAX platform"
git remote add origin https://github.com/YOUR_USERNAME/max-university.git
git branch -M main
git push -u origin main
```

### 3. Включите GitHub Pages

1. Перейдите в `Settings` → `Pages`
2. Выберите `Source: GitHub Actions`
3. Сохраните

### 4. Дождитесь деплоя

- Перейдите в `Actions` → дождитесь завершения workflow
- Ваша ссылка: `https://YOUR_USERNAME.github.io/max-university/`

### 5. Обновите URL в main.py

Замените `YOUR_USERNAME` на ваш GitHub username во всех местах в `main.py`:

```python
# Найдите и замените:
"https://YOUR_USERNAME.github.io/max-university/?role=student"
# на:
"https://ваш_username.github.io/max-university/?role=student"
```

### 6. Закоммитьте изменения

```bash
git add main.py
git commit -m "Update URLs with GitHub username"
git push
```

## 📋 Что нужно для анкеты

1. **Ссылка на репозиторий GitHub**: `https://github.com/YOUR_USERNAME/max-university`
2. **Ссылка на мини-приложение**: `https://YOUR_USERNAME.github.io/max-university/`
3. **Docker-образ**: Используйте `Dockerfile` и `Dockerfile.frontend`
4. **Requirements.txt**: Уже готов в корне проекта
5. **README**: Уже готов с полными инструкциями

## 🐳 Запуск через Docker (для проверки)

### Бэкенд:
```bash
docker build -t max-university-backend -f Dockerfile .
docker run -p 8000:8000 max-university-backend
```

### Фронтенд:
```bash
docker build -t max-university-frontend -f Dockerfile.frontend .
docker run -p 80:80 max-university-frontend
```

### Или через Docker Compose:
```bash
docker-compose up --build
```

## 📝 Команды для сборки и запуска контейнера

Все команды описаны в README.md в разделе "Docker команды".

## ⚠️ Важные замечания

1. **HTTPS обязателен**: GitHub Pages предоставляет HTTPS автоматически
2. **CORS**: Убедитесь, что бэкенд разрешает запросы с GitHub Pages домена
3. **Токен бота**: Укажите токен бота MAX в переменных окружения или в `main.py`
4. **SECRET_KEY**: Измените в `main.py` на уникальный ключ

## 📚 Дополнительные файлы

- `README.md` - Полная документация проекта
- `DEPLOY.md` - Подробная инструкция по деплою
- `QUICK_START.md` - Быстрый старт для деплоя

## 🎯 Концепция проекта

Платформа-конструктор для вузов:
- Администраторы настраивают блочную структуру
- Каждая роль видит только свои блоки
- Универсальное решение для любых вузов
- Масштабируемая архитектура

## ✨ Готово к отправке!

Проект полностью готов для отправки на хакатон. Все необходимые файлы на месте, инструкции написаны, функционал реализован.

