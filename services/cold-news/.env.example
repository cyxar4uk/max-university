# Подключение к MongoDB (коллекция news_posts).
# Если не задано — API запустится, но лента будет пустой, список источников — из дефолта.
MONGOdb=mongodb://localhost:27017/cold_news
# Или с логином: mongodb://user:password@host:27017/cold_news

# Порт Feed API (по умолчанию 3001)
# FEED_API_PORT=3001
